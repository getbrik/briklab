{
  "name": "node-full-cve",
  "version": "1.0.0",
  "description": "Node.js fixture with a known-vulnerable transitive dep (brace-expansion 5.0.5, GHSA-jxxr-4gwj-5jf2). Brik-scan is expected to fail on this project.",
  "main": "src/index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/index.js --minify",
    "test": "node test/index.test.js",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "esbuild": "^0.27.0",
    "eslint": "^10.1.0"
  },
  "license": "MIT"
}
