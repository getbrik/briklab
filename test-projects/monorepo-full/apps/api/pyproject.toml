[project]
name = "monorepo-api"
version = "0.1.0"
description = "Monorepo-full python API sub-project"
requires-python = ">=3.10"
dependencies = ["pytest"]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
