version: 1

project:
  name: node-deploy-failure
  stack: node
  stack_version: "22"

release:
  strategy: semver
  tag_prefix: v

quality:
  lint:
    enabled: false

test:
  framework: npm

package:
  docker:
    image: registry.briklab.test:5050/brik/node-deploy-failure
    dockerfile: Dockerfile

publish:
  docker:
    image: registry.briklab.test:5050/brik/node-deploy-failure

deploy:
  workflow: trunk-based
  environments:
    staging:
      target: k8s
      namespace: brik-nonexistent
      manifest: k8s/deployment.yaml
      strategy: rolling
