version: 1
project:
  name: node-no-package
  stack: node
  stack_version: "22"
quality:
  lint:
    enabled: true
    tool: eslint
security:
  deps:
    severity: high
  secrets: {}
test:
  framework: npm
# No .package block -- brik-package and brik-container-scan must NOT be instantiated (P0 gating)
