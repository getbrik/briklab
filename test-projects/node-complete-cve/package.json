{
  "name": "node-complete-cve",
  "version": "1.0.0",
  "description": "Complete Node.js fixture combining a known-vulnerable transitive dep (brace-expansion 5.0.5) and prettier-flagged format defects. Brik-lint and brik-scan are expected to fail on this project.",
  "main": "src/index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/index.js --minify",
    "test": "jest",
    "lint": "eslint src/",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "esbuild": "^0.27.0",
    "eslint": "^10.0.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "prettier": "^3.4.0"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all",
    "printWidth": 100
  },
  "license": "MIT"
}
